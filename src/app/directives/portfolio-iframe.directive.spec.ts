import { PortfolioIframeDirective } from './portfolio-iframe.directive';

describe('PortfolioIframeDirective', () => {
  it('should create an instance', () => {
    const directive = new PortfolioIframeDirective();
    expect(directive).toBeTruthy();
  });
});
