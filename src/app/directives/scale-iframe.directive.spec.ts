import { ScaleIframeDirective } from './scale-iframe.directive';

describe('ScaleIframeDirective', () => {
  it('should create an instance', () => {
    const directive = new ScaleIframeDirective();
    expect(directive).toBeTruthy();
  });
});
